#include <stdio.h>
#include "mesh.h"

int main(void)
{
    // mesh_t *mesh = mesh_create_from_file("../mesh1.obj");
    // printf("%p\n", (void *)mesh);
    // printf("%lu %lu\n", mesh->attributes_sizes.vertices_size, mesh->attributes_sizes.indices_size);
    // for (size_t i = 0; i < mesh->attributes_sizes.vertices_size; ++i)
    //     printf("%f %f %f\n", mesh->attributes.vertices[i][0], mesh->attributes.vertices[i][1],  mesh->attributes.vertices[i][2]);
    
    // for (size_t i = 0; i < mesh->attributes_sizes.indices_size; ++i)
    //     printf("%d %d %d\n", mesh->attributes.indices[i][0], mesh->attributes.indices[i][1],  mesh->attributes.indices[i][2]);
    // mesh_destroy(mesh);
    return 0;
}